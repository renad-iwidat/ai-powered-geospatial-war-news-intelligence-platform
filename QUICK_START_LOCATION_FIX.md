# تشغيل سريع: إصلاح المواقع 🚀

## الخطوة 1: تنظيف البيانات

```bash
cd /path/to/project
python scripts/cleanup_locations.py
```

**المتوقع:**
```
❌ حذف المواقع الخاطئة...
  ✓ تم حذف: بنان (TN)
  ✓ تم حذف: ندر عباس (IR)
  ... (15 موقع)

✏️ تصحيح الإحداثيات الخاطئة...
  ✓ تم تصحيح: دمشق (SY)
  ✓ تم تصحيح: بغداد (IQ)
  ... (25 موقع)

✅ تم إكمال التنظيف بنجاح!
```

---

## الخطوة 2: إعادة معالجة الأخبار

```bash
python scripts/reprocess_news.py
```

**المتوقع:**
```
🔄 بدء إعادة معالجة الأخبار...

🗑️ حذف الأحداث والمقاييس القديمة...
  ✓ تم حذف 3500 مقياس
  ✓ تم حذف 1200 حدث

🌍 إعادة معالجة الأماكن...
  ✓ معالجة 100 خبر
  ... (معالجة دفعات)

📊 إعادة معالجة المقاييس...
  ✓ معالجة 100 حدث
  ... (معالجة دفعات)

✅ تم إكمال إعادة المعالجة بنجاح!
```

---

## الخطوة 3: التحقق

```bash
# اختبر API
curl "http://localhost:7235/api/v1/news-events?country_code=LB&limit=5"
```

**المتوقع:**
```json
{
  "items": [
    {
      "id": 1,
      "place_name": "لبنان",
      "country_code": "LB",
      "location_name": "Lebanon",
      "metrics_count": 3
    }
  ],
  "total": 50,
  "limit": 5,
  "offset": 0
}
```

---

## ✅ تم!

- ✓ تم تنظيف البيانات الخاطئة
- ✓ تم تصحيح الإحداثيات
- ✓ تم إعادة معالجة الأخبار
- ✓ النظام جاهز للاستخدام

---

## 📝 ملاحظات

- **الوقت:** قد تستغرق إعادة المعالجة بعض الوقت حسب عدد الأخبار
- **الإحداثيات:** الآن مضمونة الصحة (من القائمة البيضاء)
- **الأداء:** أسرع من قبل (بدون طلبات Nominatim الإضافية)

---

## 🆘 إذا حدثت مشكلة

```bash
# تحقق من الاتصال بقاعدة البيانات
psql -U user -d database_name -c "SELECT COUNT(*) FROM locations;"

# تحقق من السجلات
tail -f app.log | grep -i location

# أعد التشغيل
python scripts/cleanup_locations.py
python scripts/reprocess_news.py
```

