"""
=============================================================================
Application Settings & Configuration
=============================================================================
يحتوي على جميع متغيرات البيئة والإعدادات المركزية للتطبيق.
يتم تحميل القيم من ملف .env
=============================================================================
"""

import os
from dotenv import load_dotenv

# ============================================================================
# Load Environment Variables
# ============================================================================
# تحميل ملف .env الذي يحتوي على البيانات الحساسة والإعدادات
load_dotenv()

# ============================================================================
# Database Configuration
# ============================================================================
# رابط الاتصال بقاعدة البيانات PostgreSQL
# الصيغة: postgresql://user:password@host:port/database
DATABASE_URL = os.getenv("DATABASE_URL")

# ============================================================================
# News View Configuration
# ============================================================================
# اسم الـ VIEW في قاعدة البيانات الذي يحتوي على الأخبار
# يستخدم في endpoints /news/snapshot و /news/list
NEWS_VIEW_NAME = os.getenv("NEWS_VIEW_NAME", "")

# ============================================================================
# Processing Configuration
# ============================================================================
# عدد الثواني بين كل polling cycle (للـ background jobs)
POLL_SECONDS = float(os.getenv("POLL_SECONDS", "2"))

# الحد الأقصى لعدد الأخبار في snapshot endpoint
MAX_SNAPSHOT_LIMIT = int(os.getenv("MAX_SNAPSHOT_LIMIT", "200"))